#!/bin/bash
set -e
set -x
ROOT="$(dirname "$0")/.."
cd "$ROOT"
eval $(docker-machine env)
number_of_images=$(($(docker images | wc -l)-1))

docker rmi -f $(docker images | sed 2,"$number_of_images"p | awk '{ print $3 }')
