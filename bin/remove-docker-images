#!/bin/bash
set -e
ROOT="$(dirname "$0")/.."
cd "$ROOT"
eval $(docker-machine env)
number_of_images=$(($(docker images | wc -l)-1))

set -x
docker rmi -f $(docker images | sed -n 2,"$number_of_images"p | awk '{ print $3 }')
